$I->wantTo('get {{ refName }} with {{ idName }} 1');
$I->haveHttpHeader('Content-Type', 'application/json');
$I->sendGET('/{{ refName }}/1');
$I->seeResponseCodeIs(HttpCode::OK);
$I->seeResponseContainsJson([
    'data' => [
        '{{ refName }}' => [
{% for key, entity in validEntityArray %}
            '{{ key|underscore }}' => '{{ entity }}',
{% endfor %}
        ]
    ]
]);
