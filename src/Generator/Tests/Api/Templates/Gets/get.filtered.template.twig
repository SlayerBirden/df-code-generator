$I->wantTo('get {{ pluralRefName }} filtered by {{ filterName }}:{{ filterValue }}');
$I->haveHttpHeader('Content-Type', 'application/json');
$I->sendGET('/{{ pluralRefName }}?f[{{ filterName }}]={{ filterValue }}');
$I->seeResponseCodeIs(HttpCode::OK);
$I->seeResponseContainsJson([
    'data' => [
        'count' => {{ filterCount }},
    ]
]);
