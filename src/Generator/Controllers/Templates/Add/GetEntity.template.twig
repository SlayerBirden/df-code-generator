$entity = new {{ entityClassName }}();
$this->hydrator->hydrate($data, $entity);

{% if relations %}
{% for relation in relations %}
//TODO add relations for {{ relation.target }} which is {{ relation.type }}
{% endfor %}
{% endif %}

return $entity;
